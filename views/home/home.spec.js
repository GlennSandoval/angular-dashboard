'use strict';

describe('snDash.home module', function() {

    beforeEach(module('snDash.home'));

    describe('home controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var view2Ctrl = $controller('HomeCtrl');
            expect(view2Ctrl).toBeDefined();
        }));

    });
});
